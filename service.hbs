<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ML Dental X-Ray Services</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #2563eb;
            --secondary-color: #10b981;
            --accent-color: #6366f1;
            --text-color: #1f2937;
            --background-color: #f0f4f8;
            --card-background: white;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        @keyframes slideIn {
            from { opacity: 0; transform: translateX(-50px); }
            to { opacity: 1; transform: translateX(0); }
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', 'Arial', sans-serif;
            line-height: 1.6;
            background-color: var(--background-color);
            color: var(--text-color);
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 50px;
            animation: fadeIn 1s ease-out;
        }

        .header h1 {
            font-size: 2.5rem;
            color: var(--primary-color);
            font-weight: 700;
        }

        .services {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
        }

        .service-card {
            background-color: var(--card-background);
            border-radius: 15px;
            padding: 30px;
            text-align: center;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            transition: all 0.4s ease;
            animation: slideIn 0.8s ease-out;
            position: relative;
            overflow: hidden;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.4s ease;
        }

        .service-card:hover::before {
            transform: scaleX(1);
        }

        .service-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.15);
        }

        .service-icon {
            font-size: 64px;
            margin-bottom: 20px;
            color: var(--primary-color);
            animation: pulse 2s infinite;
        }

        .service-icon i {
            transition: color 0.3s ease;
        }

        .service-card:hover .service-icon i {
            color: var(--accent-color);
        }

        .tabs {
            margin-top: 50px;
        }

        .tab-buttons {
            display: flex;
            justify-content: center;
            margin-bottom: 30px;
            animation: fadeIn 1.2s ease-out;
        }

        .tab-button {
            padding: 12px 25px;
            margin: 0 10px;
            background-color: transparent;
            border: 2px solid var(--primary-color);
            color: var(--primary-color);
            cursor: pointer;
            transition: all 0.3s ease;
            border-radius: 30px;
            font-weight: 600;
            position: relative;
            overflow: hidden;
        }

        .tab-button::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 0;
            background: var(--primary-color);
            transition: height 0.3s ease;
            z-index: -1;
        }

        .tab-button:hover::after {
            height: 100%;
        }

        .tab-button:hover {
            color: white;
        }

        .tab-button.active {
            background-color: var(--primary-color);
            color: white;
        }

        .tab-content {
            display: none;
            background-color: white;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            animation: fadeIn 0.6s ease-out;
        }

        .tab-content.active {
            display: block;
        }

        .tab-content ul, .tab-content ol {
            padding-left: 30px;
        }

        @media (max-width: 768px) {
            .tab-buttons {
                flex-direction: column;
            }

            .tab-button {
                margin: 5px 0;
                width: 100%;
            }
        }

        .doctors {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 40px;
}
        .doctors-container {
  animation-delay: 0.4s;
}

        .doctor-container {
  display: flex;
  justify-content: center;
  opacity: 0;
  transform: translateY(20px);
  animation: fadeInUp 0.7s ease forwards;
}



        .doctor-card {
  text-align: center;
  width: 200px;
}

.doctor-card img {
  width: 150px;
  height: 150px;
  border-radius: 50%;
  object-fit: cover;
  margin-bottom: 10px;
}

@keyframes fadeInUp {
  0% {
    opacity: 0;
    transform: translateY(20px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}
h2{
    font-weight: 500px;
    color:rgb(34, 91, 237);
    

}


    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>ML-Powered Dental X-Ray Services</h1>
        </div>

        <div class="services">
            <div class="service-card">
                <div class="service-icon">
                    <i class="fas fa-microscope"></i>
                </div>
                <h3>Advanced X-Ray Scanning</h3>
                <p>Cutting-edge digital X-ray technology with machine learning analysis.</p>
            </div>
            <div class="service-card">
                <div class="service-icon">
                    <i class="fas fa-brain"></i>
                </div>
                <h3>AI-Powered Detection</h3>
                <p>Machine learning algorithms detect dental issues with high accuracy.</p>
            </div>
            <div class="service-card">
                <div class="service-icon">
                    <i class="fas fa-chart-bar"></i>
                </div>
                <h3>Comprehensive Analysis</h3>
                <p>Detailed reports highlighting potential dental concerns.</p>
            </div>
        </div>

        

        <br>
        <br>

        <div class="doctors-container animate">
            <section class="doctors">
              <h2>Our Dental Experts </h2>
              <div class="doctor-card">
                <img src="team-1.jpg" alt="Dr. John Doe">
                <h3>Dr. John Doe</h3>
                <p>Dentist</p>
              </div>
              <div class="doctor-card">
                <img src="team-2.jpg" alt="Dr. Jane Smith">
                <h3>Dr. Jane Smith</h3>
                <p>Orthodontist</p>
              </div>
              <div class="doctor-card">
                <img src="team-3.jpg" alt="Dr. Michael Johnson">
                <h3>Dr. Michael Johnson</h3>
                <p>Periodontist</p>
              </div>
            </section>
          </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const tabButtons = document.querySelectorAll('.tab-button');
            const tabContents = document.querySelectorAll('.tab-content');
            const doctorsContainer = document.querySelector('.doctors-container');

            tabButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const tabId = button.getAttribute('data-tab');

                    // Remove active class from all buttons and contents
                    tabButtons.forEach(btn => btn.classList.remove('active'));
                    tabContents.forEach(content => content.classList.remove('active'));

                    // Add active class to clicked button and corresponding content
                    button.classList.add('active');
                    document.getElementById(tabId).classList.add('active');
                });
            });
        });
    </script>
</body>
</html>